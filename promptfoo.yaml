api:
  provider: openai
  model: gpt-3.5-turbo
  apiKey: $OPENAI_API_KEY

prompts:
  - id: hello_test
    prompt: |
      Respond to: {{query}}

tests:
  - vars:
      query: 'Hello'
    assert:
      - type: contains
        value: 'hello'
